# 📦 Пошаговая инструкция по публикации на reg.ru

## Порядок действий: СНАЧАЛА готовим локально, ПОТОМ загружаем

---

## 🏠 ШАГ 1: Подготовка на локальном компьютере

### 1.1. Создайте файл `.env`

Создайте файл `.env` в корне проекта (рядом с `requirements.txt`):

```env
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=сгенерируйте-ключ-ниже
DATABASE_URL=sqlite:///student_rating.db
```

**Сгенерируйте SECRET_KEY:**
```bash
python generate_secret_key.py
```
Скопируйте сгенерированный ключ в `.env` вместо `сгенерируйте-ключ-ниже`

### 1.2. Выполните миграции локально (опционально, но рекомендуется)

Это создаст структуру БД, которую потом можно будет использовать:

```bash
# Активируйте виртуальное окружение
.venv\Scripts\activate  # Windows
# или
source .venv/bin/activate  # Linux/Mac

# Выполните миграции
flask db upgrade

# Заполните справочники (отделы, активности)
python seed_data.py
```

**Важно**: После этого у вас появится файл `student_rating.db` (если используете SQLite). 
Этот файл можно загрузить на сервер, или создать БД заново на сервере.

### 1.3. Создайте администратора (опционально, можно и на сервере)

```bash
python create_admin.py
```

**Сохраните логин и пароль!** Они понадобятся для входа в админ-панель.

---

## 📤 ШАГ 2: Подготовка файлов к загрузке

### 2.1. Что НЕ нужно загружать на сервер:

Создайте файл `.gitignore` (если его нет) или проверьте, что в нём есть:

```
__pycache__/
*.pyc
.venv/
venv/
instance/
.env  # ⚠️ ВАЖНО: .env НЕ загружаем на сервер через FTP!
```

### 2.2. Что нужно загрузить:

```
рейтинг/
├── app/                    # ✅ Загрузить всю папку
│   ├── __init__.py
│   ├── models.py
│   ├── routes_public.py
│   ├── routes_admin.py
│   ├── static/
│   └── templates/
├── migrations/             # ✅ Загрузить всю папку
├── wsgi.py                 # ✅ Загрузить
├── run.py                  # ✅ Загрузить (для тестирования)
├── create_admin.py         # ✅ Загрузить
├── seed_data.py            # ✅ Загрузить
├── generate_secret_key.py  # ✅ Загрузить
├── requirements.txt        # ✅ Загрузить
├── .htaccess               # ✅ Загрузить
└── student_rating.db       # ✅ Загрузить (если создали локально)
```

**НЕ загружайте:**
- `.env` (создадите на сервере)
- `venv/` или `.venv/` (создадите на сервере)
- `__pycache__/`
- `.git/` (если используете Git)

---

## 🚀 ШАГ 3: Загрузка на reg.ru

### 3.1. Способ загрузки: FTP/SFTP

1. **Подключитесь к серверу через FTP-клиент** (FileZilla, WinSCP, или встроенный в reg.ru):
   - Хост: обычно `ftp.ваш-домен.ru` или IP-адрес
   - Логин и пароль: из панели управления reg.ru
   - Порт: 21 (FTP) или 22 (SFTP)

2. **Загрузите файлы**:
   - Подключитесь к серверу
   - Перейдите в директорию `public_html` или `www` (для этого проекта: `/var/www/u3325711/data/www/your-efficient.ru`)
   - Загрузите все файлы из пункта 2.2, сохраняя структуру папок

3. **Проверьте структуру на сервере**:
   ```
   public_html/
   ├── app/
   ├── migrations/
   ├── wsgi.py
   ├── requirements.txt
   └── ...
   ```

### 3.2. Альтернатива: через SSH (если доступен)

Если у вас есть SSH-доступ:

```bash
# Создайте архив локально
tar -czf rating.tar.gz app/ migrations/ wsgi.py run.py create_admin.py seed_data.py generate_secret_key.py requirements.txt .htaccess

# Загрузите через SCP
scp rating.tar.gz user@your-server.ru:/var/www/u3325711/data/www/your-efficient.ru/

# На сервере распакуйте
ssh user@your-server.ru
cd /var/www/u3325711/data/www/your-efficient.ru/
tar -xzf rating.tar.gz
```

---

## 🖥️ ШАГ 4: Настройка на сервере reg.ru

### 4.1. Подключитесь к серверу

Через SSH или через файловый менеджер в панели reg.ru.

### 4.2. Создайте виртуальное окружение на сервере

```bash
# Перейдите в директорию проекта
cd /var/www/u3325711/data/www/your-efficient.ru/

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 4.3. Создайте файл `.env` на сервере

**ВАЖНО**: Создайте `.env` НАПРЯМУЮ на сервере (не загружайте локальный):

```bash
# На сервере через SSH или файловый менеджер
nano .env
# или
vi .env
```

Вставьте:
```env
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=тот-же-ключ-что-и-локально-или-новый
DATABASE_URL=sqlite:///student_rating.db
```

**Используйте тот же SECRET_KEY, что и локально, или сгенерируйте новый:**
```bash
python3 generate_secret_key.py
```

### 4.4. Выполните миграции на сервере

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Выполните миграции
flask db upgrade

# Заполните справочники
python seed_data.py
```

### 4.5. Создайте администратора на сервере (если не создали локально)

```bash
python create_admin.py
```

**Сохраните логин и пароль!**

### 4.6. Установите права доступа

```bash
chmod 755 wsgi.py
chmod 644 .env
chmod 644 student_rating.db  # если файл уже есть
chmod 755 .  # директория должна быть доступна для записи
chmod -R 755 app/
```

### 4.7. Настройте `.htaccess`

Отредактируйте `.htaccess` на сервере, заменив пути:

```apache
# Замените эти пути на реальные:
# Для reg.ru c index.wsgi (Passenger)
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /index.wsgi/$1 [QSA,PT,L]
```

**Как узнать полный путь:**
```bash
pwd  # покажет текущий путь, для проекта это /var/www/u3325711/data/www/your-efficient.ru
```

---

## ✅ ШАГ 5: Проверка

1. Откройте ваш сайт в браузере
2. Проверьте главную страницу
3. Войдите в админ-панель: `ваш-сайт.ru/admin/login`
4. Проверьте функциональность

---

## 📋 Краткая памятка: Порядок действий

```
ЛОКАЛЬНО:
1. ✅ Создать .env с SECRET_KEY
2. ✅ (Опционально) Выполнить миграции локально
3. ✅ (Опционально) Создать админа локально
4. ✅ Подготовить файлы (убрать лишнее)
5. ✅ Загрузить на сервер через FTP

НА СЕРВЕРЕ:
6. ✅ Создать venv и установить зависимости
7. ✅ Создать .env на сервере
8. ✅ Выполнить миграции
9. ✅ Создать админа (если не создали локально)
10. ✅ Настроить .htaccess
11. ✅ Установить права доступа
12. ✅ Проверить работу
```

---

## ⚠️ Важные замечания

1. **`.env` НЕ загружайте через FTP** - создавайте на сервере вручную для безопасности
2. **`venv/` НЕ загружайте** - создавайте на сервере (разные операционные системы)
3. **Используйте тот же SECRET_KEY** локально и на сервере, или разные (не критично для SQLite)
4. **Файл БД `student_rating.db`** можно загрузить, если создали локально, или создать заново на сервере
5. **Права доступа важны** - без них приложение не заработает

---

## 🆘 Если что-то не работает

1. Проверьте логи веб-сервера в панели reg.ru
2. Проверьте права доступа: `ls -la`
3. Проверьте, что `.env` существует и содержит правильные данные
4. Проверьте, что виртуальное окружение активировано при запуске
5. Убедитесь, что пути в `.htaccess` правильные

