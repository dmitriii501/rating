# Flask Configuration
FLASK_ENV=production
FLASK_DEBUG=False

# Secret Key (ОБЯЗАТЕЛЬНО измените на случайную строку!)
# Генерация: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-change-this-in-production

# Database Configuration
# SQLite (по умолчанию, отлично подходит для небольших проектов):
DATABASE_URL=sqlite:///student_rating.db

# Или PostgreSQL (опционально, для больших нагрузок):
# DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/dbname

# Или MySQL (опционально):
# DATABASE_URL=mysql+pymysql://username:password@localhost:3306/dbname

# Если DATABASE_URL не указан, будет использован SQLite по умолчанию

