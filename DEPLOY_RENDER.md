# 🚀 Деплой на Render.com

Render даёт бесплатный веб‑сервис и отдельный постоянный PostgreSQL. Ниже быстрый чек‑лист, чтобы развернуть текущий проект без ручной возни с серверами.

---

## 1. Что уже подготовлено в репо

- `Procfile` – Render автоматически использует `web: gunicorn wsgi:app --bind 0.0.0.0:$PORT`.
- `requirements.txt` – содержит `gunicorn` и `psycopg[binary]` (драйвер PostgreSQL).
- `wsgi.py` и фабрика приложения `create_app()` читают переменные окружения (`DATABASE_URL`, `SECRET_KEY`, и т.п.).

---

## 2. Создаём сервисы на Render

1. **Подключаем репозиторий**
   - Render Dashboard → *New → Web Service* → GitHub → выбираем `rating` и ветку `main`.
   - Build command: `pip install -r requirements.txt`
   - Start command: `gunicorn wsgi:app --bind 0.0.0.0:$PORT`

2. **Создаём PostgreSQL**
   - *New → PostgreSQL* → план Free.
   - После создания в карточке БД появится `External Database URL` вида  
     `postgres://USER:PASSWORD@HOST:PORT/DBNAME`.

3. **Привязываем БД к веб-сервису**
   - В настройках веб-сервиса → *Environment → Add Environment Variable → From Database*.
   - Render автоматически создаст переменные `DATABASE_URL` и `INTERNAL_DATABASE_URL`.
   - Добавляем остальные переменные вручную:
     - `FLASK_ENV=production`
     - `FLASK_DEBUG=False`
     - `SECRET_KEY=...` (генерируем через `python generate_secret_key.py`)
     - `FLASK_APP=wsgi.py`

---

## 3. Миграции и стартовые данные

После первого деплоя нужно подготовить базу (делается один раз):

1. В панели Render → *Shell* (или через Background Job):
   ```bash
   flask db upgrade
   python seed_data.py
   python create_admin.py
   ```
2. Эти команды используют `DATABASE_URL`, поэтому убедитесь, что переменные окружения сохранены перед запуском.

---

## 4. Как обновлять приложение

1. Пушим изменения в GitHub → Render автоматически перезапустит сервис.
2. Если менялись модели – снова запускаем `flask db upgrade`.
3. Логи смотрим через *Logs* в карточке сервиса.

---

## 5. Что с данными

- PostgreSQL на Render – постоянное хранилище: данные **не** стираются при деплоях/рестартах.
- Файловая система веб‑сервиса эфемерна. Поэтому:
  - Не храните `student_rating.db` локально – используем только PostgreSQL.
  - Все загрузки/файлы должны идти в внешние хранилища (S3 и т.п.), если появятся.

---

## 6. Полезные команды для Shell

```bash
# проверить подключение к базе
python - <<'PY'
from app import db, create_app
app = create_app()
with app.app_context():
    print(db.engine.url)
PY

# создать нового администратора
python create_admin.py
```

---

Готово! После выполнения шагов сайт будет доступен по адресу вида `https://rating.onrender.com`, а база — в управляемом PostgreSQL. Если нужен пример render.yaml или помощь с миграциями – дай знать.***

