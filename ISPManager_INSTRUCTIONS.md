# 📋 Инструкция по деплою на Timeweb (виртуальный хостинг)

> Подходит для аккаунтов c ISPmanager/Timeweb Panel. Корневая директория сайта: `~/web/<домен>/public_html`.

---

## Шаг 1. Готовим структуру

1. В панели Timeweb создайте сайт (раздел **Хостинг → Сайты**).
2. Откройте файловый менеджер и перейдите в `web/<домен>/public_html`.
3. Загрузите туда содержимое репозитория (папки `app/`, `migrations/`, файлы `index.wsgi`, `.htaccess`, `requirements.txt`, `create_admin.py`, `seed_data.py` и т.д.).
4. **Не загружайте** локальные `.env`, `.venv`, `__pycache__/`.

После загрузки структура в `public_html` должна быть:

```
public_html/
├── app/
├── migrations/
├── index.wsgi
├── .htaccess
├── requirements.txt
├── create_admin.py
├── seed_data.py
└── env.example.txt
```

---

## Шаг 2. Виртуальное окружение и зависимости

1. Откройте SSH (Timeweb → «SSH-доступ» → запустить терминал).
2. Перейдите в директорию проекта:
   ```bash
   cd ~/web/<домен>/public_html
   ```
3. Создайте виртуальное окружение:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

---

## Шаг 3. Настраиваем `.env`

1. В `public_html` создайте файл `.env`.
2. Пример содержимого:
   ```env
   FLASK_ENV=production
   FLASK_DEBUG=False
   SECRET_KEY=<сгенерируйте через python generate_secret_key.py>
   DATABASE_URL=sqlite:///student_rating.db
   ```
3. Права на файл: `chmod 644 .env`.

---

## Шаг 4. Миграции и данные

```bash
source venv/bin/activate
flask db upgrade
python seed_data.py
```

Для SQLite убедитесь, что файл `student_rating.db` появился в корне проекта (если используете PostgreSQL/MySQL — пропишите строку подключения в `.env`).

---

## Шаг 5. Создаём администратора

```bash
source venv/bin/activate
python create_admin.py          # ручной ввод
# или
python create_admin.py --auto   # создаёт admin / случайный пароль
```

Запишите логин и пароль.

---

## Шаг 6. Права на файлы

```bash
chmod 755 index.wsgi wsgi.py
chmod 644 .env
chmod -R 755 app/
chmod 755 public_html
```

---

## Шаг 7. Настраиваем Python в панели

1. Зайдите в **Хостинг → Сайты → ваш домен → вкладка Python**.
2. Включите Python и выберите версию (3.10+).
3. При необходимости укажите путь к `public_html/index.wsgi`.
4. Сохраните изменения и дождитесь перезапуска сайта.

Файл `.htaccess` уже настроен под Timeweb (включает `Options +ExecCGI` и прокидывает запросы в `index.wsgi`). В `index.wsgi` активируется `venv`, загружается `.env`, затем вызывается `create_app()`.

---

## Шаг 8. Проверка

1. Откройте `https://<домен>/`.
2. Зайдите в `https://<домен>/admin/login`.
3. Если видите «Сайт не настроен» — очистите кеш, подождите 3–5 минут или перезапустите сайт в панели.

---

## Шаг 9. Логи

- Error log: `~/logs/<домен>.error.log`
- Access log: `~/logs/<домен>.access.log`

Команда для просмотра:
```bash
tail -n 100 ~/logs/<домен>.error.log
```

---

## ⚠️ Частые проблемы

| Симптом | Что проверить |
| --- | --- |
| 500 Internal Server Error | Есть ли `venv`, установлены ли зависимости, правильны ли права на `index.wsgi` |
| `SECRET_KEY must be set` | Файл `.env` создан? переменная прописана? |
| «Сайт не настроен» | Загружены ли файлы в `public_html`, включён ли Python в панели |
| Нет админов | Не запускали `python create_admin.py` |
| SQLite не создаётся | Права на каталог (`chmod 755 public_html`) и сам файл (`chmod 644 student_rating.db`) |

---

## 🆘 Если не запускается

1. Проверьте `tail -n 100 ~/logs/<домен>.error.log`.
2. Убедитесь, что `index.wsgi` указывает на корректный путь и имеет `application`.
3. Пересоздайте виртуальное окружение и переустановите зависимости.
4. Сбросьте настройки Python в Timeweb и снова укажите `index.wsgi`.
5. При необходимости обратитесь в поддержку Timeweb (они видят системные логи сервера).

---

Успешного деплоя! Если нужен другой способ (Timeweb Cloud Apps, VPS, Docker) — адаптируйте шаги из `DEPLOY.md`.

