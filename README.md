### Студенческий рейтинг — Flask

Минимально жизнеспособная версия сайта рейтинга с публичными страницами и админ-панелью.

- **Бэкенд**: Flask + SQLAlchemy + Flask-Migrate + Flask-Login
- **БД**: SQLite (по умолчанию) или PostgreSQL/MySQL. SQLite отлично подходит для небольших проектов.
- **Фронтенд**: Jinja2 + Bootstrap 5, кастомные бежевые тона.

---

## ☁️ Публикация на Render

> 📖 **Краткая инструкция и чек-лист**: см. `DEPLOY_RENDER.md`

Основные шаги:
- Подключить репозиторий к Render, задать build/start команды (уже описаны в `Procfile`).
- Создать PostgreSQL (данные сохраняются) и указать `DATABASE_URL`, `SECRET_KEY` и др. переменные в разделе Environment.
- После деплоя выполнить `flask db upgrade`, `python seed_data.py`, `python create_admin.py` через Render Shell.

## 🚀 Публикация на reg.ru

> 📖 **Подробная пошаговая инструкция**: См. файл `DEPLOY_STEPS.md`

### Порядок действий: СНАЧАЛА готовим локально, ПОТОМ загружаем на сервер

### 🏠 ЛОКАЛЬНО (на вашем компьютере):

1. **Создайте файл `.env` в корне проекта** (скопируйте из `env.example.txt` или создайте вручную):

```env
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=ваш-случайный-секретный-ключ-минимум-32-символа
# Для SQLite (по умолчанию, не требует настройки):
DATABASE_URL=sqlite:///student_rating.db
# Или для PostgreSQL (опционально):
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/dbname
```

**Важно**: Сгенерируйте безопасный SECRET_KEY:
```bash
python generate_secret_key.py
# или
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

2. **(Опционально) Выполните миграции локально**:
```bash
flask db upgrade
python seed_data.py
```

3. **(Опционально) Создайте администратора локально**:
```bash
python create_admin.py
```
Скрипт автоматически сгенерирует безопасный логин и пароль. **Сохраните их!**

4. **Подготовьте файлы к загрузке**:
   - Убедитесь, что `.env` НЕ будет загружен (создадите на сервере)
   - Убедитесь, что `venv/` или `.venv/` НЕ будет загружен (создадите на сервере)
   - Можно загрузить `student_rating.db` (если создали локально) или создать на сервере

5. **Загрузите файлы на сервер через FTP/SFTP**:
   - Подключитесь к серверу через FTP-клиент (FileZilla, WinSCP)
   - Загрузите все файлы проекта в `public_html` или `www`, сохраняя структуру папок
   - **НЕ загружайте**: `.env`, `venv/`, `__pycache__/`

### 🖥️ НА СЕРВЕРЕ (reg.ru):

6. **Создайте виртуальное окружение на сервере**:
```bash
python3 -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
```

7. **Создайте файл `.env` на сервере** (через SSH или файловый менеджер):
   - Используйте тот же SECRET_KEY или сгенерируйте новый через `python generate_secret_key.py`
   - **ВАЖНО**: Создавайте `.env` НАПРЯМУЮ на сервере, не загружайте локальный!

8. **Выполните миграции БД на сервере**:
```bash
flask db upgrade
python seed_data.py
```

9. **Создайте администратора на сервере** (если не создали локально):
```bash
python create_admin.py
```

10. **Настройте WSGI** (отредактируйте `.htaccess` на сервере):
   - Если используется Apache с mod_wsgi, отредактируйте `.htaccess`:
     - Замените `/path/to/venv` на реальный путь к виртуальному окружению
     - Замените `/path/to/project` на реальный путь к проекту
   - Если используется Passenger, настройте `PassengerAppRoot` и `PassengerPython`

11. **Установите права доступа**:
```bash
chmod 755 wsgi.py
chmod 644 .env
# Если используете SQLite:
chmod 644 student_rating.db
chmod 755 .  # Директория должна быть доступна для записи
```

### Альтернативный вариант: использование Gunicorn

Если reg.ru поддерживает Gunicorn, создайте файл `gunicorn_config.py`:

```python
bind = "127.0.0.1:8000"
workers = 4
timeout = 120
```

И запустите:
```bash
gunicorn -c gunicorn_config.py wsgi:app
```

---

## 💻 Локальная разработка

#### Требования

- Python 3.10+
- PostgreSQL (локально опционально; можно начать с SQLite по умолчанию)

#### Установка

```bash
python -m venv .venv
.venv\Scripts\activate  # Windows
# или
source .venv/bin/activate  # Linux/Mac
pip install -r requirements.txt
```

Создайте файл `.env` в корне проекта (или оставьте по умолчанию - SQLite будет использован автоматически):

```env
FLASK_ENV=development
FLASK_DEBUG=True
SECRET_KEY=dev-secret-key-change-me
# DATABASE_URL можно не указывать - будет использован SQLite по умолчанию
# DATABASE_URL=sqlite:///student_rating.db
```

#### Миграции БД

```bash
flask db init
flask db migrate -m "init"
flask db upgrade
```

#### Инициализация справочников

```bash
python seed_data.py
```

Добавит отделы и базовые активности.

#### Создание администратора

**Рекомендуется использовать скрипт для создания безопасного админа**:
```bash
python create_admin.py
```

Или старый способ (только если админов еще нет):
```bash
curl -X POST -d "username=admin&password=admin" http://127.0.0.1:5000/admin/bootstrap_admin
```

#### Запуск

```bash
python run.py
```

Откройте `http://127.0.0.1:5000`.

---

## 📄 Страницы

**Публичные:**
- `/` — Главная
- `/rating` — Общий рейтинг
- `/departments` — Список отделов
- `/departments/<id>` — Рейтинг отдела
- `/newbies` — Рейтинг новичков (по семестру)
- `/profile/<user_id>` — Профиль участника

**Админ-панель:**
- `/admin/login` — Вход
- `/admin/` — Панель
- `/admin/users` — Участники (CRUD)
- `/admin/departments` — Отделы (CRUD)
- `/admin/activities` — Активности (CRUD)
- `/admin/scores` — Начисление баллов и лента

---

## 🔒 Безопасность

- ✅ Используйте сильный `SECRET_KEY` (минимум 32 символа)
- ✅ Никогда не коммитьте `.env` в Git (уже добавлен в `.gitignore`)
- ✅ В production установите `FLASK_DEBUG=False`
- ✅ SQLite отлично подходит для небольших проектов (до ~1000 пользователей)
- ✅ Для больших нагрузок рассмотрите PostgreSQL или MySQL
- ✅ Регулярно делайте резервные копии файла `student_rating.db` (если используете SQLite)
- ✅ Регулярно обновляйте зависимости
- ✅ Используйте HTTPS в production
- ✅ Создавайте администраторов через `create_admin.py` с безопасными паролями

---

## 📝 Структура проекта

```
рейтинг/
├── app/
│   ├── __init__.py          # Инициализация Flask приложения
│   ├── models.py            # Модели БД
│   ├── routes_public.py     # Публичные маршруты
│   ├── routes_admin.py      # Админ-маршруты
│   ├── static/
│   │   └── style.css        # Стили
│   └── templates/           # Шаблоны Jinja2
├── migrations/              # Миграции Alembic
├── wsgi.py                  # WSGI entry point для production
├── run.py                   # Development server
├── create_admin.py          # Скрипт создания админа
├── seed_data.py             # Инициализация справочников
├── requirements.txt         # Зависимости
├── .env.example             # Пример переменных окружения
├── .htaccess                # Конфигурация Apache
└── README.md                # Документация
```


